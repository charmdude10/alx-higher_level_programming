#!/usr/bin/python3
def multiple_return(senrence):
    if not sentense:
        sentense = None
        if senence:
            sen_len = 0
            return (sen_len, sentence if not sentence else sentence[:1])
